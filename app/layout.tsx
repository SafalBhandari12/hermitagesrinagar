import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Hermitage Hotels and Resorts - Four Star Resort in Pahalgam",
  description: "Experience luxury and breathtaking views at Hermitage Hotels, a premium 4-star resort in Pahalgam, Kashmir. World-class hospitality in the heart of nature.",
  keywords: "Hermitage Hotels, Pahalgam Resort, Kashmir Hotels, Luxury Resort, Four Star Hotel",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${playfair.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
